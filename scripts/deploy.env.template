# === Application ===
NODE_ENV=${NODE_ENV}
PORT=${PORT}

# === Docker Compose ===
COMPOSE_PROJECT_NAME=${COMPOSE_PROJECT_NAME}
APP_EXTERNAL_PORT=${APP_EXTERNAL_PORT}
DOCKER_BUILD_TARGET=${DOCKER_BUILD_TARGET}

# === Vite (baked in at image build time) ===
VITE_API_BASE_URL=${VITE_API_BASE_URL}
VITE_CLIENT_TIMEOUT=${VITE_CLIENT_TIMEOUT}
VITE_SERVER_TIMEOUT=${VITE_SERVER_TIMEOUT}
